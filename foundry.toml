[profile.default]
src = "src"
test = "test"
out = "out"
cache_path = "cache"
libs = []
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200
via_ir = true

# ponytail: chain IDs auto-detected from RPC; hardcode only in deploy script
[profile.bohr_testnet]
eth_rpc_url = "https://rpc.bohr.life"

[profile.bot_mainnet]
eth_rpc_url = "https://rpc.botchain.ai"
